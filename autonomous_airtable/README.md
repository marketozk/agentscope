# 🤖 Автономная система массовой регистрации Airtable

## 📋 Описание

Полностью автономная система для массовой регистрации аккаунтов Airtable через браузер.

### ✨ Основные возможности

- ✅ **Без API ключей** - всё через браузер с Camoufox (Firefox anti-detect)
- ✅ **Уникальный fingerprint** на каждую регистрацию
- ✅ **Автоматическое получение данных** с randomdatatools.ru
- ✅ **Временная почта** с temp-mail.org
- ✅ **Автоматическое подтверждение email**
- ✅ **Persistent профили** с изоляцией между итерациями
- ✅ **Профессиональная архитектура** с переиспользуемым фреймворком

## 🏗️ Архитектура

### Модули

- `autonomous_registration_loop.py` - основной скрипт регистрации
- `fingerprint_generator.py` - генератор уникальных отпечатков браузера
- `profile_manager.py` - менеджер браузерных профилей
- `browser_framework/` - переиспользуемый фреймворк для работы с браузером
  - `browser_agent.py` - обёртка над Playwright + Camoufox
  - `steps.py` - паттерн шагов с ретраями и диагностикой

### Шаговый паттерн

Все операции выполняются через `BrowserStep` с:
- ✅ Автоматическими ретраями при ошибках
- ✅ Детальным логированием контекста
- ✅ Сохранением скриншотов при падении
- ✅ Дампом HTML страниц для анализа

## 🚀 Запуск

### Через BAT файл (проще)

```cmd
start.bat
```

### Через командную строку

```cmd
cd "c:\Users\regis\OneDrive\Рабочий стол\Проект AgentScope"
.venv\Scripts\python.exe autonomous_airtable\autonomous_registration_loop.py
```

## 📊 Результаты

Все результаты сохраняются в `Browser_Use/registration_results/`:
- JSON файлы с полными данными
- TXT файлы для удобного просмотра
- Скриншоты и HTML дампы в `Browser_Use/debug_screenshots/` при ошибках

## 🔧 Настройки

Реферальная ссылка задаётся в `main()`:
```python
referral_url = "https://airtable.com/invite/r/LzgpVACU"
```

Задержка между циклами (в секундах):
```python
await system.run_infinite_loop(delay_between_cycles=60)
```

## 📝 Логи

Каждый шаг логируется с контекстом:
```
▶️ Шаг: get_random_data | контекст: {'iteration': 1}
✅ Шаг успешно завершён: get_random_data (попытка 1)
```

При ошибках сохраняются:
- Скриншоты: `debug_screenshots/step_name_attemptN_timestamp.png`
- HTML: `debug_screenshots/step_name_fail_timestamp.html`

## 🛡️ Anti-Detection

- Camoufox (Firefox-based браузер с встроенным anti-detect)
- Уникальный fingerprint на каждую итерацию
- Persistent профили с полной изоляцией
- Реалистичное поведение: прогрев браузера, человекоподобный ввод
- Случайные задержки между действиями

## ⚠️ Остановка

Нажмите `Ctrl+C` для корректной остановки системы с выводом статистики.
