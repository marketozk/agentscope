# 🎯 Промпт для анализа WhatsApp скриншота

## 📋 Полная версия промпта

```text
🤖 ЗАДАЧА: Анализ интерфейса WhatsApp на Android

📱 КОНТЕКСТ:
- Перед тобой скриншот приложения WhatsApp на телефоне Android
- Разрешение экрана стандартное для мобильного устройства
- Интерфейс на русском языке

🎯 ЦЕЛЬ:
Написать сообщение в чат с именем: "Т В"

📋 ЧТО НУЖНО СДЕЛАТЬ:

1. **АНАЛИЗ ТЕКУЩЕГО СОСТОЯНИЯ**
   - Определи, на каком экране мы находимся (список чатов, открытый чат, другое)
   - Виден ли чат "Т В" на экране?
   - Если чат открыт - где находится поле ввода сообщения?
   - Есть ли поиск чатов? Где он находится?

2. **ОПРЕДЕЛЕНИЕ КООРДИНАТ**
   - Укажи примерные координаты (X, Y) для каждого важного элемента
   - Координаты в процентах от размера экрана (0-100%)
   - Например: "Поле поиска: X=50%, Y=15%"

3. **ПЛАН ДЕЙСТВИЙ**
   Составь пошаговый план действий для достижения цели:
   
   Шаг 1: [Действие]
      - Тип действия: click / type / scroll / etc.
      - Координаты: X=..%, Y=..%
      - Описание: что произойдет
   
   Шаг 2: [Действие]
      ...
   
   И так далее до достижения цели

4. **АЛЬТЕРНАТИВНЫЕ ВАРИАНТЫ**
   - Если есть несколько способов достичь цели - опиши их
   - Укажи самый быстрый и самый надежный путь

5. **ВОЗМОЖНЫЕ ПРОБЛЕМЫ**
   - Что может пойти не так?
   - Как проверить, что мы на правильном пути?

📊 ФОРМАТ ОТВЕТА:
Предоставь структурированный анализ с четкими координатами и описанием действий.
Используй эмодзи для наглядности.

🔍 ВАЖНО:
- Будь максимально конкретным с координатами
- Учитывай, что UI может иметь анимации и задержки
- Описывай действия так, чтобы их мог выполнить бот

Начни анализ!
```

---

## 🔧 Настройка промпта

### Переменные для изменения:

1. **Целевой чат**: `"Т В"` → замените на нужное имя
2. **Язык интерфейса**: `"русском языке"` → при необходимости измените
3. **Тип устройства**: `"Android"` → можно указать iOS

### Модификации промпта:

#### Для другого языка:
```python
def build_analysis_prompt(self, target_chat: str, language: str = "ru") -> str:
    languages = {
        "ru": "русском языке",
        "en": "English",
        "uk": "українською мовою"
    }
    
    # В промпте заменить:
    # - Интерфейс на {languages[language]}
```

#### Для специфичной задачи:
```python
# Изменить секцию "ЦЕЛЬ" на:
🎯 ЦЕЛЬ:
{custom_goal}

# Например:
custom_goal = "Найти сообщение от вчерашнего дня"
custom_goal = "Открыть настройки группы"
custom_goal = "Переслать сообщение в другой чат"
```

---

## 📊 Ожидаемый формат ответа модели

### Структура анализа:

```
1. 🔍 АНАЛИЗ ТЕКУЩЕГО СОСТОЯНИЯ
   └─ Описание экрана
   └─ Видимые элементы
   └─ Текущее положение

2. 📍 КООРДИНАТЫ ЭЛЕМЕНТОВ
   └─ Чат "Т В": X=50%, Y=65%
   └─ Поле поиска: X=50%, Y=8%
   └─ Кнопка "Новый чат": X=85%, Y=88%

3. 📋 ПЛАН ДЕЙСТВИЙ
   
   Шаг 1: Открыть целевой чат
      • Тип: click
      • Координаты: X=50%, Y=65%
      • Ожидаемый результат: Откроется окно чата
   
   Шаг 2: Найти поле ввода
      • Тип: locate
      • Координаты: X=50%, Y=92%
      • Ожидаемый результат: Курсор в поле ввода
   
   Шаг 3: Написать сообщение
      • Тип: type
      • Текст: "Ваше сообщение"
      • Ожидаемый результат: Текст появится в поле
   
   Шаг 4: Отправить
      • Тип: click
      • Координаты: X=90%, Y=92%
      • Ожидаемый результат: Сообщение отправлено

4. 🔄 АЛЬТЕРНАТИВНЫЕ ВАРИАНТЫ
   
   Вариант A (быстрый):
   - Если чат виден → сразу клик
   
   Вариант B (через поиск):
   - Клик на поиск → ввод имени → выбор чата

5. ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ
   
   Проблема 1: Чат не виден
   └─ Решение: Использовать скролл или поиск
   
   Проблема 2: Поле ввода заблокировано
   └─ Решение: Проверить права доступа к чату
   
   Проблема 3: Неточные координаты
   └─ Решение: Использовать OCR для точного поиска
```

---

## 🎨 Примеры использования

### Пример 1: Базовый анализ

```python
analyzer = WhatsAppAnalyzer()

result = analyzer.analyze_screenshot(
    image_path="screenshots/whatsapp_main.jpg",
    target_chat="Т В"
)

print(result['analysis'])
```

### Пример 2: С кастомным промптом

```python
class CustomWhatsAppAnalyzer(WhatsAppAnalyzer):
    def build_analysis_prompt(self, target_chat: str) -> str:
        return f"""
        Проанализируй скриншот WhatsApp.
        Цель: Написать сообщение в чат "{target_chat}"
        
        Дай короткий план из 3-5 шагов с координатами.
        """

analyzer = CustomWhatsAppAnalyzer()
result = analyzer.analyze_screenshot(...)
```

### Пример 3: Множественные чаты

```python
chats = ["Т В", "WhatsApp Business", "Step 1", "Step 2"]

for chat_name in chats:
    result = analyzer.analyze_screenshot(
        image_path="screenshots/whatsapp_main.jpg",
        target_chat=chat_name
    )
    print(f"\n=== Анализ для чата: {chat_name} ===")
    print(result['analysis'])
```

---

## 🔍 Детальный разбор секций

### 1. АНАЛИЗ ТЕКУЩЕГО СОСТОЯНИЯ

**Что модель анализирует:**
- Тип экрана (главная / чат / настройки)
- Видимые UI элементы
- Активные состояния (открыто / свернуто)
- Положение прокрутки

**Ключевые вопросы:**
- На каком экране мы?
- Что видно?
- Где целевой чат?

### 2. ОПРЕДЕЛЕНИЕ КООРДИНАТ

**Формат координат:**
```
X: горизонтальная ось (0% слева → 100% справа)
Y: вертикальная ось (0% сверху → 100% снизу)
```

**Примеры:**
- Центр экрана: X=50%, Y=50%
- Верхний левый угол: X=10%, Y=10%
- Нижний правый угол: X=90%, Y=90%

### 3. ПЛАН ДЕЙСТВИЙ

**Типы действий:**
- `click` - одиночный клик
- `long_press` - долгое нажатие
- `type` - ввод текста
- `scroll` - прокрутка
- `swipe` - свайп (влево/вправо)
- `search` - использование поиска

**Структура шага:**
```
Шаг N: [Название действия]
   • Тип: [тип действия]
   • Координаты: X=%, Y=%
   • Параметры: [доп. параметры]
   • Результат: [ожидаемое состояние]
```

### 4. АЛЬТЕРНАТИВНЫЕ ВАРИАНТЫ

**Критерии выбора:**
- Скорость выполнения
- Надежность
- Количество шагов
- Зависимость от UI состояния

### 5. ВОЗМОЖНЫЕ ПРОБЛЕМЫ

**Типичные риски:**
- Неточность координат
- Изменение UI
- Сетевые задержки
- Блокировки/права доступа

---

## 💡 Советы по оптимизации

### Улучшение точности:

1. **Качество скриншота**
   - Разрешение: минимум 720p
   - Формат: PNG или JPEG
   - Яркость: хорошо читаемый текст

2. **Контекст**
   - Указывайте больше деталей о состоянии
   - Упоминайте специфичные элементы
   - Добавляйте информацию о версии WhatsApp

3. **Промпт**
   - Будьте конкретны в цели
   - Добавляйте ограничения (например, "за максимум 5 шагов")
   - Указывайте приоритеты (скорость vs надежность)

### Обработка ошибок:

```python
result = analyzer.analyze_screenshot(...)

if result['status'] == 'success':
    # Парсинг плана действий
    plan = parse_action_plan(result['analysis'])
    
    # Проверка валидности координат
    if validate_coordinates(plan):
        execute_plan(plan)
    else:
        # Повторный анализ с уточнением
        result = analyzer.analyze_screenshot(..., retry=True)
```

---

## 📚 Дополнительные ресурсы

- [Gemini Computer Use Documentation](https://ai.google.dev/gemini-api/docs/computer-use)
- [WhatsApp UI Guidelines](https://www.whatsapp.com/design)
- [Android UI Automation](https://developer.android.com/training/testing/ui-automator)

---

**Автор:** AgentScope Project  
**Версия:** 1.0  
**Дата:** 26 октября 2025
